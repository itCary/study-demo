server:
  port: 80

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql:///springboot_test
    username: root
    password: root

  redis:
    host: localhost
    port: 6379

logging:
  level:
    online.goudan: debug
    org.springframework: info

mybatis:
  type-aliases-package: online.goudan.pojo
  configuration: # 控制台输出执行sql

    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
